---
paths:
  - "PairShot/Views/**/*.swift"
  - "PairShot/ViewModels/**/*.swift"
---
- View must only reference ViewModel (direct Service access prohibited)
- ViewModel must use @Observable macro (iOS 17+)
- @State is for View-internal UI state only
- Heavy computation must be in .task {} or ViewModel (never in View body)
- Use NavigationStack (NavigationView is deprecated)
- Minimum tap target size: 44pt (field workers may wear gloves)
- Minimize text guidance — prefer visual icons + haptic feedback
