# v1.2.1 — 2026-05-30

**Build**: 1
**Range**: v1.2.0...v1.2.1
**Compare**: https://github.com/KangGyeongGu/pair-shot-iOS/compare/v1.2.0...v1.2.1

---

## Highlights

- **첫 실행 Paywall 가독성 정리**: "무료 버전으로 계속 (하루 페어 5개 · 광고 표시)" 안내가 회색에 묻혀 잘 안 보이던 문제를 본문 톤으로 또렷하게 표시. 동시에 "구매 시 Apple ID 청구 / 해지 / 환불" 약관 안내가 무료 진입로 아래로 내려와 사용자가 무료 선택을 먼저 인지한 뒤 약관을 읽도록 위계 정리.
- **카메라 진입 시 콘솔 SF Symbol 경고 4건 제거**: 하단바의 홈·설정 아이콘 렌더링 초기 layout pass 에서 `glyphPointSize == 0` 으로 CoreUI lookup 이 실패하던 경고 (`'house.fill' / 'gearshape' not found in system symbol set`) 잠재 진단 노이즈 정리. 화면 동작·아이콘 표시에는 변화 없음.

---

## Added

None.

## Changed

- 첫 실행 Paywall 의 "무료 버전으로 계속" 안내를 secondary 회색에서 primary 본문 톤으로 강조, Apple ID 청구·해지·환불 안내는 그 아래로 이동. Apple `SubscriptionStoreView` 의 내장 CTA / 구독 복원 / 약관 영역은 미변경. (`paywall`)

## Fixed

- 카메라 진입 직후 콘솔에 출력되던 `'house.fill' / 'gearshape' ... No symbol named ... found in system symbol set` 경고 4건 제거. `CameraBottomBar` 의 아이콘 폰트 사이즈 계산에 1pt 하한을 보장해 SwiftUI 초기 layout pass 의 `size = 0` 으로 인한 SF Symbol lookup 실패를 차단. 실제 layout 안정 후 동작·표시 동일. (`camera`)

## Removed

None.

## Internal

- `ExportSettingsViewModel` 단위 테스트 39건 추가 (State 22 / Preset actions 17). 라인 커버리지 45.16% → 66.36%, `+Preset` extension 100% 도달. 공통 helper 를 `ExportSettingsViewModelTestSupport` 로 분리해 SwiftLint `type_body_length` 임계 (300 lines) 준수. (`test`)

---

## Compatibility

- **iOS**: 17.0+
- **Device**: iPhone (Portrait only)
- **Data migration**: 없음 — SwiftData SchemaV1 유지, MigrationVerificationTests 4/4 PASS

## Known Issues

None.

---

## Commits

- a23af26 fix(camera): CameraBottomBar 초기 layout pass SF Symbol size 0 로그 제거
- c7f2bb4 refactor(paywall): 무료버전 계속 안내 강조 + Apple ID 안내 순서 교체
- 84d8d9e Merge pull request #10 from KangGyeongGu/feature/export-settings-vm-coverage
- 925c98e test(export): ExportSettingsViewModel 단위 테스트 39건 추가
